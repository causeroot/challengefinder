To compile webSearch.c first make sure you have the openssl developer libraries. Then
download the Google API secret from Passpack and save it as google.apikey

Once completed you can build using:

buildWithSecret
